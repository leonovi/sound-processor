import { createOutputId } from 'utils/createOutputId';

export const DEFAULT_VALUE = 333;
export const DEFAULT_MIN_VALUE = 0;
export const DEFAULT_MAX_VALUE = 1000;

export const CONSTANT_SOURCE_OUTPUT = {
  id: createOutputId('constant_source'),
};
