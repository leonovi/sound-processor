import { createInputId } from 'utils/createInputId';
import { createOutputId } from 'utils/createOutputId';

export const GAIN_INPUT = { id: createInputId('gain') };
export const GAIN_OUTPUT = { id: createOutputId('gain') };
