import { nanoid } from 'nanoid';

export const GAIN_INPUTS = [{ id: `GAIN_INPUT-${nanoid()}`, label: 'Input' }];

export const GAIN_OUTPUTS = [
  { id: `GAIN_OUTPUT-${nanoid()}`, label: 'Output' },
];
